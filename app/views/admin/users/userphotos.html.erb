
<div class="container">
  <div class="container">
    <br>
    <h3 class="box"><%= @user.name %>'s Photo<span class="count-font">　<<%= @photo_images_count %>photos></span></h3>
  </div>
</div>

<div class="container">
  <div class="row">
      <%= render 'admin/photo_images/index', photo_images: @photo_images %>
  </div>
</div>

<div class="container">
  <%= link_to "#{@user.name}'s pageへ戻る", admin_user_path(@user.id), class: "btn btn-outline-lightgreen" %>
</div>