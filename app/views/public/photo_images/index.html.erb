<h1>ALL</h1>
<% @photo_images.each do |photo_image| %>

  <%= image_tag photo_image.get_image, size: '200x100' %><br>
  <%= link_to "編集", edit_photo_image_path(photo_image.id) %><br>
  <%= photo_image.created_at.strftime('%Y/%m/%d %H:%M') %><br>
<% end %>


