<h4>PHOTO</h4>

<%= form_with model: @photo_image do |f| %>

  <%= f.file_field :image, accept: "image/*" %>
  <h4>title</h4><%= f.text_field :photo_title %>
  <h4>caption</h4><%= f.text_area :photo_caption %>
  <%= f.submit '投稿' %>
<% end %>